Patterns

+ Min/Max
+ Sum
	+ Init at 0
	+ Loop over all values
	+ Examples
		+ Triangular
		+ GC
		+ gcwin
+ Product
	+ Init at 1
	+ loop over all values
	+ Examples
		+ Factorial
		+ Viterbi
		+ PWM

+ Loop w/ Short-circut
	+ Init at True/False
	+ Check values, break on something
	+ Examples
		+ prime
		+ transmembrane
		+ saving throws
		+ birthday
+ Search
	+ Examples
		+ restriction digest
+ Sort - too boring
+ DP - too advanced for this class


# primes

def isprime(i):
	for j in range(i+1, n):
		if i % j == 0:
			return False
	return True


for i in range(2, n):
	if isprime(i): print(i, '*')
	else: print(i)

